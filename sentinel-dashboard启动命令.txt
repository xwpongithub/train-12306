一定要用dos窗口启动，而不是用power shell，不然会报错
java -Dserver.port=18080 -Dcsp.sentinel.dashboard.server=localhost:18080 -Dproject.name=sentinel-dashboard -jar sentinel-dashboard-1.8.7.jar


流控效果：
warm-up 预热
设单机阈值为100，coldFactor为3
指的是经过多长时间才主键升至设定的QPS阈值，比如阈值是100，时长为100秒，则从33开始经过10秒上升到100
排队等待

流控模式
直接
关联 （例如：支付接口出问题了要限制下单接口的流量）
链路 从特定入口进来时需要限流

流控模式：
熔断：
  配置项
     最大响应时间(RT) 200(单位:ms)
     比例阈值 0.3
     熔断时长 熔断多长时间（单位:s）
     最小请求数 6
     统计时长 1s
  在1秒内最小请求数大于等于6个时才触发熔断规则，之后会判断最大响应时间大于等于200ms的请求数比例是否大于等于
  比例阈值的0.3，大于才熔断
